import 'package:flutter/material.dart';

class MusicPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Music Generator'),
      ),
      body: Center(
        child: Text(
          'Music Generation Feature Coming Soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
